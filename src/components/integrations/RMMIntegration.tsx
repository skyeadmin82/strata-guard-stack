import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function RMMIntegration() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>RMM Integration</CardTitle>
        <CardDescription>Configure RMM integrations</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Integration configuration coming soon...</p>
      </CardContent>
    </Card>
  );
}