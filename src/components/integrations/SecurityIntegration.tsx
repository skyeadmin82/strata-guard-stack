import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function SecurityIntegration() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Security Integration</CardTitle>
        <CardDescription>Configure security integrations</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Integration configuration coming soon...</p>
      </CardContent>
    </Card>
  );
}